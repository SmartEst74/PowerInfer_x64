[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-linux-gnu"]

# For rust-gpu kernel compilation (when implemented), use:
#   rustup run nightly-2025-06-23 cargo build --features cuda
# Or set RUSTUP_TOOLCHAIN=nightly-2025-06-23
